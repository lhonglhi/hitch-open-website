---
deployment:
  tasks:
    - export DEPLOYPATH=$HOME/public_html/hitchopen.ai
    # *.html 用通配符：改版新增 hope.html / racing.html 时曾漏掉，
    # 导致部署清单与仓库不同步。以后再加页面无需改这里。
    - /bin/cp -R *.html .htaccess css data images js video $DEPLOYPATH/

# push 到 production 远端会自动触发本文件的部署任务
